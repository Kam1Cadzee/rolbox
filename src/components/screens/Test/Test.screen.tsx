import React from 'react';
import {SafeAreaView} from 'react-native';

interface ITestScreenProps {}

const TestScreen = ({}: ITestScreenProps) => {
  return <SafeAreaView />;
};

export default TestScreen;
